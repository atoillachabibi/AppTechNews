import React from 'react'
import VerticalList from './VerticalList';

const SoftwareNews = ({ data }) => {
  return <VerticalList title="Software News" data={data} />;
};

export default SoftwareNews;
